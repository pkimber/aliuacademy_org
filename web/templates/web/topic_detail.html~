{% extends 'web/base_sidebar.html' %}

{% block sub_title %}
  Topic
{% endblock sub_title %}

{% block sub_heading %}
  Topic
{% endblock sub_heading %}

{% block content %}

    <h1 class="content-head">{{ topic.name }}</h1>


    <video width="550" controls>
    <source src="{{ topic.video.url }}" type="video/mp4">
    <source src="{{ topic.video.url }}" type="video/ogg">
    Your browser does not support the video tag.
    </video>

    <div style="margin-top:30px;">
    <a href="{{ topic.video.url }}" download="{{ topic.download_file_name }}" class="pure-button">
    <i class="fa fa-cloud-download"></i> Download
    </a>
    </div>

{% endblock content %}


{% block sidemenuitems %}

    <li><a href="{% url 'web.university.department.list' topic.course.department.university.slug %}">&nbsp;&nbsp;{{ topic.course.department.university.name }} University</a></li>
    <li><a href="{% url 'web.department.course.list' topic.course.department.pk %}">&nbsp;&nbsp;&nbsp;&nbsp;{{ topic.course.department.name }}</a><li>
    <li><a>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;{{ topic.course.name }}</a><li>
    <li class="pure-menu-selected"><a>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;{{ topic.name }}</a><li>
    <li class="pure-menu-heading">All Topics</li>

    {% for topic in topic_list %}
    <li><a href="{% url 'web.topic.detail' topic.pk %}">{{ topic.name }}</a></li>
    {% endfor %}

{% endblock sidemenuitems %}
